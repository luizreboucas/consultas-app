import PacienteModel from "@/models/PacienteModel";

describe('testes de request de pacientes', () => {
    it('should return a array of pacientes', () => {
        let pacientes;

        expect(pacientes).not.toBe(undefined)
    })

    it('should return a single row of pacientes', () => {
        let paciente;

        expect(paciente).not.toBe(undefined)
    })

    it('should update a paciente', ()=>{
        let paciente;
        expect(paciente).not.toBe(undefined)
    })

    it('should return a deleted paciente', ()=>{
        let paciente;
        expect(paciente).not.toBe(undefined)
    })
})